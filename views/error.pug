extends layout.pug

block content
    nav.grid-container.portfolio-breadcrumb
        a(href='/') ← Back
    article.grid-container.portfolio-intro
        div
            h1 #{error.status}
            p.lead.text-light #{error.message}
            br
            p.lead.text-light #{error.stack}